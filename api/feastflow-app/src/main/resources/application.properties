spring.application.name=feastflow-app

# MongoDB connection URI (override with env var MONGODB_URI)
spring.data.mongodb.uri=mongodb+srv://dhruvesh:dhruv%402150@cluster0.gdtzqq8.mongodb.net/?appName=Cluster0
spring.data.mongodb.database=feastflow

# JWT settings (use env vars to override in production)
# Base64-encoded secret. To generate: `openssl rand -base64 32`
security.jwt.secret=${SECURITY_JWT_SECRET:ZmVhc3RmbG93LWRlZmF1bHQtand0LXNlY3JldC1rZXk=}
# Expiration in milliseconds (default 1 hour)
security.jwt.expirationMillis=${SECURITY_JWT_EXPIRATION:3600000}
security.jwt.refreshExpirationMillis=${SECURITY_JWT_REFRESH_EXPIRATION:604800000}

# Enable only once to migrate existing plaintext passwords to BCrypt on startup
# security.password.migrate=true

# CORS settings
frontend.origin=${FRONTEND_ORIGIN:http://localhost:4200}
frontend.origin.extra=${EXTRA_FRONTEND_ORIGIN: https://feastflow-app.web.app}

